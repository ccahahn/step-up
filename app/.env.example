# Neon → your project → Connection string (pooled). Server-only: this never
# reaches the browser, and there is no public key to worry about.
# Leave it unset locally and the app writes to ../data/local.json instead.
DATABASE_URL=postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
